{
  "name": "@yunzone-infer/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "协议契约单一事实源（JSON Schema）+ codegen → TS 类型 + C++ 结构（DESIGN D15）",
  "scripts": {
    "codegen": "node scripts/codegen.mjs",
    "build": "pnpm run codegen",
    "ts-check": "tsc --noEmit"
  },
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.0.3",
    "json-schema-to-typescript": "^15.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
