基础工程：DCinfer（推理图编排运行时）
调度器策略参考：既有成熟 C++ 调度器实现（策略借鉴，不引入代码依赖）
文档性质：立项开题。仅界定主旨、范围与阶段；技术选型另行专项讨论。
1. 项目背景
既有工程已具备三项核心资产：
DCinfer：进程内推理图编排运行时。提供图拓扑抽象、数据驱动的并发执行、引擎统一注册、图序列化能力，且远程节点与本地节点在图级语义上可保持一致。
DCIr：推理图编辑器。将图序列化和反序列化，并将用到的模型打包储存。
DCNet：张量网络传输框架。已完成"出站方向"建设——远端服务可被包装为图中的一等节点，传输、编解码、错误归一化均有稳定契约。
现状的边界：推理执行仍限于单机，或"单机调用外部服务"。算力无法池化——持有多样模型与硬件的机器之间，没有组织、发现与协同机制。本项目的出发点：把分散的算力组织成可被统一检视、统一调度的推理资源池，且以叠加层方式实现，不侵入两个基础工程的本体。
2. 项目目标与定位
一句话目标：构建"服务器持有推理图、客户端持有算力"的分布式推理系统——服务器检视系统中已登记的全部节点，确认具备任务所需能力后启动工作流，并为每个（客户端， 任务）二元组提供结果回收通道。定位：
面向推理任务的分发与协同，不是通用计算平台，不涉足训练。
3. 核心概念
概念	主旨定义
服务器	推理图的唯一持有者；节点注册信息的检视者；工作流的启动者；结果的回收与聚合者
客户端	模型与硬件资源的持有者；将自身包装为网络节点，主动声明能力；任务的执行者；结果的回推者
能力	客户端对"我能执行什么"的结构化声明：可承载的引擎类型、模型、端口形状规则、并发容量
任务	调度的最小单位；（客户端， 任务）二元组是结果回收的基本键
工作流	服务器完成能力检视后启动的一次图级执行：分区 → 绑定 → 派发 → 回收 → 聚合
4. 工作流主旨
登记：客户端上线，向服务器注册自身能力声明，并维持存活信号；
检视：服务器接到推理请求后，将图所需能力与已登记节点做匹配；不满足则拒绝启动，满足则进入工作流；
绑定：服务器将图的执行职责划分并绑定到具体客户端；
派发：服务器向客户端下发任务，随附该（客户端， 任务）对应的结果回收地址；
执行与回推：客户端完成本地执行后，向回收地址推送结果；
聚合：服务器将回收结果汇入图执行流，直至产出最终输出。
5. 可行性结论
经对既有资产的核实，结论为：可行，且承重结构已就位。已具备：
图的持有、序列化与重建能力；
节点远程化的抽象与契约——远程节点与本地节点图级语义一致，上层调度零特判；
网络错误到图级语义的归一化体系；
张量线上格式的基础版本（高效版本已在既有路线图中）。
需要新建（全部位于叠加层）：
注册中心：能力登记、存活视图、下线处理；
调度器：能力检视、图分区、任务到客户端的绑定；
客户端执行代理：接收任务、驱动本地执行、回推结果；
结果回收通道：按（客户端， 任务）二元组寻址与接收。
6. 主要风险与对策（主旨级）
风险	对策
客户端入站方向是全新的可靠性边界，错误语义若与既有归一化体系脱节，将放大为模糊故障	入站错误的编码规则作为早期专项设计，与既有归一化表对齐
任务时长差异大，单一的等待/回收语义难以兼顾短任务效率与长任务资源占用	采用两阶段策略：先以简单语义闭环，再按实测引入异步回收
部分图级语义（如执行互斥）只在进程内成立，跨机拆分后失效	调度层承担约束：有互斥关系的节点必须绑定同一客户端
客户端异构且可能随时掉线，影响在途任务	检视前置拒绝 + 存活检测 + 任务级重调度兜底
7. 验收主旨
端到端正确性：多机环境下，图任务从派发至聚合的结果与单机执行一致；
语义一致性：远程节点在图级的行为（状态、错误、诊断）与本地节点无差别；
容错可预期：掉线、超时、过载场景下系统行为明确，不产生悬挂任务与歧义结果。
8. 与基础工程的关系
本系统只复用、不修改 DCinfer 与 DCNet 的既有接口与语义；
建设中若发现具有通用性的新需求（如更高效的传输格式），以提案形式反馈基础工程路线图，不在本项目中私自分叉。