# ── 依赖 ──
node_modules/

# ── Next.js 构建产物 ──
.next/
out/
next-env.d.ts
*.tsbuildinfo

# ── 环境变量（含密钥，禁止入库）──
.env
.env.*
!.env.example

# ── 测试与覆盖率 ──
coverage/

# ── 契约 codegen 产物（构建期生成，不入库；DESIGN D15）──
contracts/generated/

# ── C++ / CMake / vcpkg ──
build/
build-*/
cmake-build-*/
CMakeUserPresets.json
vcpkg_installed/
*.o
*.obj
*.a
*.lib
*.so
*.dylib
*.dll
*.exe

# ── 系统文件 ──
.DS_Store
Thumbs.db
