{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RegisterResponse",
  "description": "控制通道注册响应。失败时 HTTP 4xx + ErrorEnvelope（errors 域）；成功返回本结构。",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "ok"],
  "properties": {
    "version": {
      "const": 1,
      "type": "integer",
      "description": "control-channel 域契约版本"
    },
    "ok": {
      "type": "boolean",
      "description": "注册是否成功"
    },
    "heartbeatIntervalSeconds": {
      "type": "integer",
      "minimum": 1,
      "description": "server 下发的心跳间隔建议（V6：默认 30s / TTL 90s 可配置；缺省时 client 取本地默认值）"
    }
  }
}
